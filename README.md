Métodos e Atributos Estáticos

Os atributos quando declarados e pertencentes à uma classe, eles compartilham a mesma ideia e o mesmo valor em todas as instâncias declaradas;

Os Métodos são funções em que pertencem a uma mesma classe que podem ser chamadas diretamente sem a criação de um objeto.
